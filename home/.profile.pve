# Guard variable to ensure sourcing only once
if [ -n "${SOURCED_PROFILE_PVE}" ]; then
  return 0 # Exit the script if already sourced
fi

# Set the guard variable
SOURCED_PROFILE_PVE=1

PROMPT='%n@%m:%~%# '

#clear
echo
fastfetch
echo
